[default]
extend-ignore-identifiers-re = [
    "(?i)mosquitto", # name of https://github.com/eclipse-mosquitto/mosquitto
    # Intentionally mirrors the (misspelled) attribute name in geventhttpclient's
    # UserAgent, which is what decides whether redirects are followed
    "(?i)redirect_resonse_codes",
]